# SECURITY REVIEW — аудит чувствительного материала (TASK-TB-03)

Дата: 2026-09-19. Второй, независимый аудит: проверка того, что
чувствительный материал Telegram-экспорта не попал в публичный репозиторий,
и укрепление парсера против будущих утечек. Значения секретов нигде в этом
файле (и в логах аудита) не воспроизводятся — только категории и счётчики.

## Что проверялось

1. Все файлы `catalog/techno-bypass/` (19 MD), `tools/parse_telegram_export.py`,
   документация, изменённая TASK-TB-01 (README/AGENTS/ROADMAP/SOURCES/Каталог).
2. **Git-история**: полный текст всех строк, добавленных пятью локальными
   коммитами (`f2673a2..HEAD`: 8c1f4a5, 5e91638, 9b5ecac, 46ad38a, 07da70b).
3. Состав закоммиченных файлов: 39 `.md`, 5 `.sh`, 1 `.py`, 3 без расширения
   (init-скрипты NVR/scripts) — JSON-датасеты, HTML-экспорт и дайджесты
   в git отсутствуют.

## Метод 1 — точные значения (самый сильный контроль)

Из корпуса программно извлечены 30 уникальных значений-кандидатов
(категории: mtproto-secret ×15, psk ×4, url-user ×4, vpn-server-ip ×3,
url-pass ×2, proxy-uri-credential ×1, private-channel-id ×1; источники —
регулярки по тексту и href всех 1161 сообщений). Каждое значение искалось
буквально в: (а) каждом закоммиченном файле рабочего дерева, (б) каждой
строке, добавленной локальными коммитами.

**Результат: 0 совпадений.** Ни одно известное значение секретов корпуса
не содержится в дереве или в истории коммитов каталога.

## Метод 2 — консервативный шаблонный скан

По всем новым файлам и добавленным строкам: приватные пути Telegram (`c/`,
прокси-ссылки с `secret=`, `vless/vmess/ss/trojan/…://`, `PSK:`, `Пароль/Логин:`,
`BEGIN PRIVATE KEY`, `user|pass=` в URL, userinfo@, high-entropy-токены
длиной ≥32 вне публичных URL-контекстов.

**Результат: 0 находок**, кроме двух намеренных документационных
упоминаний-описаний категорий (в легендах — без значений; формулировки
не образуют паттерн целиком).

## Аудит парсера и укрепление (внесено этим аудитом)

Парсер по построению не пишет Markdown и не печатает значений; его выход —
рабочий датасет вне git. Тем не менее TASK-TB-03 добавил:

1. **Расширенные категории детекции** (было 5 → стало 11): явный
   `mtproto-proxy-secret` (tg:// и t.me/proxy), русскоязычные
   `Пароль/Логин`, `psk-assignment` (в т.ч. «Общий ключ PSK»),
   `url-embedded-credentials` (user/pass в параметрах и userinfo@),
   `vpn-server-credentials` (IKEv2-конфики). На том же корпусе: psk теперь
   детектируется (0 → 5 сообщений; ручная находка TB-01 подтверждена
   автоматикой), mtproto-секреты выделены в отдельную категорию.
2. **Красные флаги в датасете**: каждое сообщение `messages.json` получает
   `sensitive_flags` — только имена категорий, без значений (39 сообщений).
3. **Редакция дайджеста**: текстовое превью помеченного сообщения в
   `digest.txt` заменяется на `[sensitive: <категории> — preview omitted]`
   (39 строк; проверено — маркеры значений в редактированных строках
   отсутствуют).
4. **Самотест** `--selftest`: синтетические плейсхолдеры (не реальные
   секреты) проверяют детекцию всех новых категорий, редакцию дайджеста и
   обязательное НЕ-помечание обычных публичных GitHub-ссылок.
   Текущий прогон: PASS.
5. Аргументация CLI изменена на `--out <dir> files…` с `--selftest`
   (ранее `--out` был обязательным всегда).

Инварианты сохранены: обычные публичные URL проектов не отбрасываются и не
помечаются; счётчики чувствительного печатаются только по категориям;
детерминизм (повторный прогон на том же экспорте дал идентичные метрики
корпуса: 1161 сообщений, 2426 вхождений, 1632/1093 уникальных).

## Текущие счётчики корпуса (укреплённый парсер, категории/сообщения)

telegram-private-link 10 · mtproto-proxy-secret 11 · password-assignment 15 ·
password-ru 8 · psk-assignment 5 · login-ru 4 · url-embedded-credentials 3 ·
vpn-server-credentials 5 · proxy-credential-scheme 7 ·
subscription-token-url 0 · private-key-block 0.
Категории пересекаются (одно сообщение может нести несколько).
Вручную дополнительно известно (не паттернами): утёкшая платная подписка —
1 сообщение; модифицированный APK на файлообменнике — 1 сообщение.

## Остающиеся ограничения

- Паттерны — эвристика: не поймут перефразированные креды, скриншоты
  (фото-вложения в экспорт не входят), base64-блоб без маркеров.
- `messages.json` намеренно содержит полный сырой текст (нужен для
  куризации) — защищён только конвенцией «рабочий каталог вне git»;
  флаги `sensitive_flags` делают риск случайного копирования заметным.
- High-entropy-скан по необходимости с белым списком публичных контекстов
  (github/gist/habr/…) — теоретически может пропустить токен, замаскированный
  под путь на известном домене.
- Экспорт сам по себе остаётся единственным полным источником сырья; при
  новых экспортах повторять прогон парсера и этот аудит.

## Вывод

Утечек нет: 0 точных совпадений значений, 0 паттерн-находок в дереве и
истории. Парсер укреплён для будущих импортов; самотест доступен одной
командой (`python3 tools/parse_telegram_export.py --selftest`).
